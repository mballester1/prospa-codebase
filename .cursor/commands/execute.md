Now implement precisely as planned, in full.

**Before any implementation:** In each git repo you touch, sync with the remote so you are not coding on a stale branch — run `git fetch`, then `git pull --ff-only` on the current branch (or merge/rebase `origin/main` first if this ticket requires tracking latest `main`; follow the repo’s usual convention and fix conflicts before proceeding).

Implementation Requirements:

- Write elegant, minimal, modular code.
- Adhere strictly to existing code patterns, conventions, and best practices.
- Include thorough, clear comments/documentation within the code.
- As you implement each step:
  - Update the markdown tracking document with emoji status and overall progress percentage dynamically.
